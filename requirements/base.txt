# --- jádro -------------------------------------------------------------
Django>=5.1,<5.2
django-environ>=0.11            # konfigurace z proměnných prostředí
psycopg[binary]>=3.2            # PostgreSQL
cryptography>=42                # šifrování oddělené identity

# --- fronta úloh (import, generování reportů) --------------------------
celery>=5.4
redis>=5.0

# --- provoz ------------------------------------------------------------
gunicorn>=22.0; sys_platform != "win32"   # na Windows neběží, tam se vyvíjí přes runserver
whitenoise>=6.7                 # statické soubory bez samostatného webserveru

# --- úložiště raw souborů (S3 / MinIO) ---------------------------------
django-storages>=1.14
boto3>=1.34

# --- data a grafy ------------------------------------------------------
pandas>=2.2
numpy>=1.26
openpyxl>=3.1                   # import stávajících Excelů
# Plotly se jako knihovna pro Python nepoužívá: figura se skládá jako
# obyčejný slovník a vykreslí ji plotly.js v prohlížeči (static/vendor/).

# --- reporty -----------------------------------------------------------
# Generování PDF je v requirements/pdf.txt zvlášť: WeasyPrint potřebuje
# systémové knihovny, které se na Windows instalují špatně. Bez něj
# aplikace funguje a zprávu ukáže v HTML – jen nevyrobí PDF.

# --- přidat, až se budou používat -------------------------------------
# django-tables2               tabulky se serverovým stránkováním
# django-filter                filtry nad seznamy
# django-crispy-forms          vzhled formulářů
# pgvector                     embeddingy článků (fáze 4)
